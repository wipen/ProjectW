<?xml version="1.0" encoding="utf-8"?>
<manifest package="cn.wipen.projectw"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:name=".WApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/StartStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".Activity.PathMeasureExampleActivity">
        </activity>
        <activity android:name=".Activity.RoundImageViewActivity">
        </activity>
        <activity android:name=".Activity.MemoryTestActivity">
        </activity>
    </application>

</manifest>